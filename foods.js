// const pgraph = document.querySelector('body > div:nth-child(9) > h3:nth-child(2)');

// console.log(pgraph);

// const pgraphs = document.querySelectorAll('div');

/* pgraphs.forEach(pgraph => {
    console.log(pgraph);
}); */

const pgraph = document.querySelector('p');

console.log(pgraph.innerText);

// pgraph.innerText += ' Select Below.';

